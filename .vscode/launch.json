{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "gdb",
      "request": "launch",
      "preLaunchTask": "${defaultBuildTask}",
      "name": "Launch Program",
      "target": "${workspaceRoot}/lib/schema-salad-d-test-library",
      "cwd": "${workspaceRoot}",
      "valuesFormatting": "parseText"
    }
  ]
}
